# TalentHub Open Questions

## Canonical active state (ADR-004 Accepted 2026-08-05 — replaces prior pre-acceptance state)

- **FASE 0 COMPLETE.**
- **GAP ANALYSIS APPROVED**; companion ADR **Accepted**.
- **ADR-004 rev. 4.2 Accepted**; owner acceptance completed 2026-08-05.
- **OWNER ACCEPTANCE COMPLETED**.
- **CORE SCHEMA DESIGN (Gate B1) — READY / OPENED DOCUMENTALLY**. B1 is not implemented.
- **CORE MIGRATION READINESS (Gate B2) — BLOCKED**. B2 is a prerequisite of any migration; staging is **not** a B1 requirement.
- **B2 future risk / criterion:** the Core migration plan must include a concurrent-transfer test for the at-least-one active-owner invariant. `student_profiles.school_id` remains compatibility-only and is reconciled with `student_enrollments` by one synchronization layer; it is not authority. This does not change the conceptual decision or authorize SQL.
- **PUBLISHING READY WITH DEFERRED ITEMS** (documentary).
- **INTERACTIONS BLOCKED exclusively D-OD-1** (Gate C is blocked exclusively by D-OD-1 as a decision; C.1 / C.2 / C.5 are preparation controls, not additional decisions).
- **GATE D MIGRATIONS: BLOCKED** (until A / B1 / C / B2 closed; Gate D is never "in parallel" with A, B1, B2 or C).
- **MIGRATIONS BLOCKED** / **IMPLEMENTATION BLOCKED** / **SUPABASE UNCHANGED**.
- **D-01..D-43 fixed and preserved**; **D-OD-1..D-OD-7 deferred and open**.
- **Canonical gate sequence:** **A → B1 → C → B2 → D**. B2, C and D remain blocked; C is blocked exclusively by D-OD-1. No B2/C/D gate is opened by this acceptance.

## Q1 - Supabase staging verification

- Type: External validation.
- Status: Open.
- Apply the contact-routing, interview privacy and interview status-transition migrations to staging.
- Verify company minor contact requests, school approval/rejection, message soft-locking, interview INSERT invariants, immutable identity fields and allowed status transitions.

## Q2 - Deployment configuration

- Type: Operational.
- Status: Open.
- Configure `SEED_SECRET` in every deployed environment and confirm `/api/seed` returns `503` when deployment configuration is incomplete.

## Q3 - Dependency vulnerabilities

- Type: Maintenance.
- Status: Open.
- Triage the 20 vulnerabilities (1 low, 9 moderate, 10 high) reported by `npm audit --omit=optional` inside `apps/web` in a controlled maintenance change. The workspace root audit reports 0 vulnerabilities because its only dev-dependency is the Supabase CLI, which has no reported advisories in this tree. Re-verified on 2026-08-05 in `docs/technical/KNOWN_ISSUES.md` under "Current Verification".

## Q4 - Schema snapshot drift

- Type: Maintenance.
- Status: Open.
- Keep migrations canonical and schedule a complete regeneration of `supabase/schema.sql` for historical sections still out of sync.

## Q5 - Automated runtime tests

- Type: Quality.
- Status: Open.
- Add disposable Supabase integration tests for authorization-sensitive paths after staging roles and fixtures are defined.

## Q6 - Identity / organizations / resource ownership — ADR acceptance (rev. 3, four-gate model)

> **HISTORICAL — SUPERSEDED BY REV 4.1 (Q10, this session).** The rev. 3 framing in this Q6 entry is preserved for traceability but is **not** the canonical state. Specifically, the rev. 3 claims inside this Q6 — **D-OD-2 / D-OD-3 / D-OD-6 as independent gates for Gate C** — are **not** part of the canonical state; under rev. 4 / 4.1, D-OD-2 and D-OD-3 are **sub-decisions absorbed by D-OD-1** (the chosen alternative encodes the `student_id` / `applicant_id` and `proposed_amount` choices), and D-OD-6 / D-OD-7 are **implementation / runtime notes, not independent gates** in this classification. The canonical state is recorded in Q10 and in `docs/workflow/STATUS.md` "Identity / Organizations / Resource Ownership docs correction (rev. 4.1, second-audit residue cleanup, this mission)". The four-gate model and the rev. 3 / rev. 4 corrections remain as historical.

- Type: Architectural (documentation, historical).
- Status: **Addressed (rev. 3, historical).** Rev. 3 corrections applied 2026-08-05. The ADR is **Proposed — OWNER ACCEPTANCE REQUIRED**; the rev. 3 update does **not** auto-Accept. **Superseded by Q10 (rev. 4.1).**
- Companion: `docs/architecture/IDENTITY_ORGANIZATION_GAP_ANALYSIS.md` and `docs/architecture/ADR/ADR-004-identity-organizations-and-resource-ownership.md`.
- The four-gate model is **non-circular**: Gate A (ADR conceptual), Gate B (Core schema-only, D-OD-1 not required), Gate C (Interactions schema-only, D-OD-1 required), Gate D (migraciones ejecutables, runtime + staging + production). The ADR can move Proposed → Accepted at Gate A without D-OD-1, migrations authored, staging applied, runtime RLS or production. The acceptance criteria are in ADR-004 §15.A and the gap analysis §17.A.
- Resolve the open decisions in the gap analysis §16 and ADR-004 §14 (D-OD-1..D-OD-7) **on their respective gates**. D-OD-1 is the only blocker for Gate C (Interactions schema-only). **D-OD-2 and D-OD-3 are sub-decisions absorbed by D-OD-1, not independent gates** (rev. 4 / 4.1). **D-OD-6 is an implementation / runtime note, not an independent gate** (rev. 4 / 4.1). D-OD-5 is a gate for Gate B. D-OD-4 is the **operational** choice for service-role backfilled `created_by_profile_id` (the structural decision — `NOT NULL` for new writes, no `ON DELETE SET NULL`, audit only — is recorded in the gap analysis §10.2, §13.4, §7.4 and the ADR §6.10 / §7.6 / §8 I-12, I-15, I-19). **D-OD-7 is an implementation / runtime note, not an independent gate** (rev. 4 / 4.1; payload / link redaction for minor-targeted notifications and the no-new-channel policy; the audit does **not** propose a trigger that blocks legitimate notifications to the student).
  - **D-OD-1.** Applications / proposals structural alternative (A, B, C, D in gap analysis §8.4 / §8.6). **Not a gate for ADR-004 acceptance (Gate A). The only blocker for Gate C (decision).**
  - **D-OD-2.** Whether `applications.student_id` is kept alongside `applicant_id`. **Sub-decision absorbed by D-OD-1; not an independent gate.**
  - **D-OD-3.** Whether `proposals.proposed_amount` becomes required for `freelance_request`. **Sub-decision absorbed by D-OD-1; not an independent gate.**
  - **D-OD-4.** Operational choice for `created_by_profile_id` on service-role backfilled rows. Recorded (operational, not a structural decision; not an independent gate in this classification).
  - **D-OD-5.** Role list for `organization_memberships` when `kind = 'company'`. Gate B.
  - **D-OD-6.** `revoked_at` and `expires_at` on `contact_requests` for D-38. **Implementation / runtime note; not an independent gate in this classification.**
  - **D-OD-7.** D-37 notifications decision: payload / link redaction for minor-targeted notifications, and the policy that notifications do not create a new contact channel outside the school-mediated path. The audit does **not** propose a trigger that blocks legitimate notifications to the student. **Implementation / runtime note; not an independent gate in this classification.**
- **Gate B (Core schema-only) is BLOCKED** by B.2 (staging) + B.3 (CI Node 22) + B.4 (surgical fixes) + B.5 (Core migration plan with baseline / backfill / RLS / rollback / staging verification) + B.6 (Core migration plan coverage). The Gate B **documentary gate is READY** (CORE DOCUMENTARY PACKAGE: READY FOR AUDIT); the **execution gate is BLOCKED** (rev. 4.1).
- **Gate C (Interactions schema-only) is BLOCKED** by C.3 (D-OD-1, the only blocker as a decision). **C.1 / C.2 / C.5 are preparation controls, not additional decisions** (rev. 4.1): C.1 (Gate A cleared) is a Gate A concern; C.2 (Gate B cleared) is a Gate B documentary / execution concern; C.5 (Interactions migration plan) is a preparation control (the plan is authored and reviewed under Gate C; the plan is not adopted until D-OD-1 is decided).
- **Gate D (migraciones ejecutables) is BLOCKED** by D.1 (Gate A) + D.2 (Gate B) + D.3 (Gate C) + D.4 (staging + fixtures) + D.5 (baseline remote reconciled) + D.6 (design audit) + D.7 (backfill, rollback, staging verification) + D.8 (RLS runtime exercised) + D.9 (production authorized) + D.10 (audit dictums closed). **Runtime grants, fixtures and the RLS runtime verification are Gate D concerns, not Gate A or Gate B concerns.**
- Until then, no migration, no SQL, no policy, no server action, no component, no test, no commit and no push are produced for the identity / organizations / resource ownership workstream. This docs pass is documentation-only. **SUPABASE UNCHANGED.**
- The free staging project and the CI Node 22 Phase 1 result remain binding preconditions for any RLS-related work; this question does not relax them.

## Q7 - Identity / Organizations / Resource Ownership docs correction (2026-08-05, this mission)

- Type: Documentation correction.
- Status: **Addressed in this mission (rev. 3).** The auditor's findings on the gap analysis and ADR-004 are addressed: the four-gate model (§17 gap / §15 ADR); the school_id matrix (gap §9.4); the XOR ownership invariant table (gap §7.4); the unique owner active flow (gap §5.4); the company_profiles formal explanation (gap §4.4); the created_by_profile_id audit-only reiteration (gap §10.2, §13.4, §7.4; ADR §7.6, §8 I-12, I-15, I-19); the views/grants/security principles and projection table (gap §13.4; ADR §7.7, §8 I-20); the applications/proposals concept vs table distinction (gap §8.6; ADR §7.4); the expand/transition/contract framework (gap §15.12; ADR §11.12) and the freelance expand/transition/contract plan (gap §15.13); the RLS runtime staging posture (gap §17.D; ADR §15.D); the revised acceptance criteria (gap §19; ADR §15).
- The historical blocks in the four workflow docs (STATUS, NEXT_ACTIONS, SESSION_LOG, OPEN_QUESTIONS) are preserved; only additive blocks are added. The historical block in NEXT_ACTIONS.md is explicitly marked as **HISTORICAL (rev. 1 docs pass, superseded by the rev. 3 follow-up block)**.
- The next session reviews the corrected gap analysis and ADR-004 and decides whether to commit the documentation diff under a `docs:` prefix. Do not push. The orchestrator runs `git diff --check`, `git status --short --branch` and `git diff --stat` only; no `npm`, no `test`, no `lint`, no `typecheck`, no `build`, no SQL, no Supabase call, no MCP mutation, no migration is executed in this docs pass.

## Q8 - Architecture audit — ADR-004 rev. 2 open observations (2026-08-05)

> **HISTORICAL — SUPERSEDED BY REV 4.1 (Q10, this session).** The rev. 3 framing in this Q8 entry is preserved for traceability but is **not** the canonical state. Specifically, the rev. 3 claims inside this Q8 — **the conceptual projection table (10 projections)** and **the `company_profiles` compatibility recommendation (single-table view, justified)** — are **not** part of the canonical state; under rev. 4 / 4.1, the public projection table is **exactly 6 rows** (the 3 authenticated projections are moved out of the public table and are a Gate D concern), and the `company_profiles` target is **Option A: physical adapter table during compatibility** (not a view; the view conversion is only after consumers, FKs and policies are migrated and verified). The canonical state is recorded in Q10 and in `docs/workflow/STATUS.md` "Identity / Organizations / Resource Ownership docs correction (rev. 4.1, second-audit residue cleanup, this mission)".

- Type: Architectural (audit follow-up, historical).
- Status: **Addressed in the rev. 3 / rev. 4 / rev. 4.1 update of the gap analysis and ADR-004 (rev. 3 / rev. 4 historical; rev. 4.1 canonical).** Pointer: see the entry `## 2026-08-05 — Architecture audit — ADR-004 rev. 2` in `docs/workflow/SESSION_LOG.md`. The open observations recorded there are closed by the rev. 3 sections in the gap analysis and the ADR; the rev. 4 and rev. 4.1 refinements are recorded in the architecture docs. The five residues flagged by the second independent audit are addressed by the rev. 4.1 corrections (Q10).
  - **FK destination for `school_id`** — closed by gap analysis §9.4 (the school_id matrix) and §4.4 (company_profiles formal). The matrix classifies each entry as Propietario / Contexto / Institución mediadora / Matrícula / Histórico / Redundante / Variable / N/A — no se crea. `profiles.school_id` is recorded as historical and never as identity or authorization. `student_profiles.school_id` is the current academic context. `school_members.school_id` maps to `organization_memberships.organization_id` with `organizations.kind = 'school'`. **`interviews.school_id` is explicitly absent in the current inventory; N/A; do not create.**
  - **Ownership constraints / XOR and unique owner** — closed by gap analysis §5.4 (the 8-step conceptual transfer flow, suspension semantics, four implementation options) and §7.4 (XOR invariant table with actor, owner, context and per-resource rule). The Company A / membership `disabled` example is recorded in gap analysis §10.2 and ADR §7.3 / §8 I-19. The rev. 4.1 correction eliminates "defer the index" and any physically invalid order, and documents the executable conceptual sequence (lock the organization, verify current owner and new profile, create / activate the new owner's membership, within the same transaction neutralize the previous owner, promote the new owner, register the audit event, verify exactly one active owner before commit; the physical order neutralize-then-promote is the opposite of the logical flow transfer-and-then-demote, and is required because the unique partial index is not deferrable). This Q10 entry is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**.
  - **`created_by_profile_id` as actor vs authorization** — closed by gap analysis §10.2, §13.4, §7.4 and ADR §7.3, §7.6, §8 I-12, I-15, I-19. The column is audit only; it is not an authorization source, not an ownership column, not a capacity column; the RLS layer does not read it.
  - **`security_invoker` and grants for views** — closed by gap analysis §13.4 and ADR §7.7, §8 I-20. The rev. 4.1 correction chooses and documents a coherent per-row security mode for the 6 public projections: all 6 public rows use the **controlled `security_definer` exception** (owner-controlled / not mutable by caller, fixed `search_path`, `security_barrier` when joined, strict allowlist, `EXECUTE` / `SELECT` only to the minimum role set: `anon` + `authenticated` only, `PUBLIC` no grant). The `security_invoker = true` flag is **not** used on any of the 6 public rows; it is reserved for the authenticated-projection list and for internal helpers, where the "anon only view" principle does not apply. The contradiction between `security_invoker = true` for those rows and the principle "anon only view, zero direct base grants" is resolved. The public projection table is **exactly 6 rows** (rev. 4 / 4.1). Runtime grants follow Gate D.
  - **Circularity between ADR and migration gates** — closed by the four-gate model (gap §17, ADR §15). Gate A is independent of Gate B, C, D. D-OD-1 is a Gate C concern (the only blocker for Gate C as a decision; C.1 / C.2 / C.5 are preparation controls, not additional decisions, per rev. 4.1), not a Gate A concern. The Gate A acceptance criteria do not require D-OD-1, migrations authored, staging applied, runtime RLS or production.
  - **`company_profiles` transition** — closed by gap analysis §4.4. The PK, the relation to `profiles` and `auth.users`, the identity of the company, the resources that reference it, the policies, the server actions, the routes, the three current uses, the target destination (`organizations(kind = 'company')`), the conversion of the admin to an active `owner` membership, the **single coherent compatibility option (Option A: physical adapter table during compatibility)** — **the current state is not a view and the target is not an immediate view** — and the no-automatic-creation rule with the **six** reviewed classes (duplicates, shared accounts, multiple accounts, companies without a user, users without a company, resources without an owner) are recorded. The adapter is retired or converted only after consumers, FKs and policies are migrated and verified.
  - **Applications / D-OD-1 not in the general baseline** — closed by gap analysis §8.6 and Gate C (§17.C / §15.C). The conceptual contract (Company uses applications; External uses opportunity_proposals; posts are not applications / proposals; applications are not `freelance_request`; proposals are not `job`, `internship`, `company_project`) is approved. The physical table (alternatives A, B, C, D) is Gate C and is the only blocker for Gate C (decision). D-OD-2 and D-OD-3 are sub-decisions absorbed by D-OD-1; D-OD-6 and D-OD-7 are implementation / runtime notes, not independent gates in this classification.
  - **Dual-write / backfill / rollback / idempotency** — closed by gap analysis §15.12. The expand / transition / contract framework is applied to every step in the migration order. The principles (additive, idempotent, observable, gradual, centralized dual-write layer, adapters, contract only at the end, no duplicates, safe re-run, checksums when viable, rejected rows recorded, no overwrite of new data) are recorded.
  - **Freelance rollback** — closed by gap analysis §15.13. The freelance rename is an expand / transition / contract plan with the contract forward-only non-destructive, the `opportunity_id` preserved, the `opportunity_proposals` table not renamed, and the counts before and after each phase recorded. §15.6 is the **summary** of the freelance rename step; §15.13 is the **detailed** expand / transition / contract plan for the same step.
  - **Staging and RLS runtime** — closed by Gate D (gap §17.D, ADR §15.D). The staging is available but may be empty during Gate B / Gate C. The fixtures catalog is the only thing the gap analysis records; the fixtures are not created. The RLS runtime verification is a Gate D step.
- The canonical state (rev. 4.1) is **FASE 0 COMPLETE**, **ADR-004 Proposed / READY FOR OWNER ACCEPTANCE**, **GAP ANALYSIS READY FOR AUDIT (APPROVED pendiente auditoría)**, **CORE DOCUMENTARY PACKAGE: READY FOR AUDIT** (CORE SCHEMA-ONLY READY), **GATE B EXECUTION: BLOCKED** (until owner acceptance / ADR Accepted and preconditions), **PUBLISHING READY WITH DEFERRED ITEMS**, **INTERACTIONS BLOCKED exclusively D-OD-1** (C.1 / C.2 / C.5 are preparation controls, not additional decisions), **GATE D MIGRATIONS: BLOCKED** (until A / B / C closed + baseline remote + backfill + rollback + RLS + staging), **MIGRATIONS BLOCKED**, **IMPLEMENTATION BLOCKED**, **SUPABASE UNCHANGED**. D-01..D-43 preserved; ADR-004 remains **Proposed — OWNER ACCEPTANCE REQUIRED**; no claim of Accepted or migrations ready. See Q10 for the full canonical state.
- The next session awaits the **owner acceptance of ADR-004 (Gate A)** and the **second independent audit of the gap analysis**. Phase 1B (schema-only) and all migrations remain **BLOCKED** by Gate B, Gate C and Gate D. No commit, no push.

## Q9 - Identity / Organizations / Resource Ownership docs correction (rev. 4 audit-preparation, 2026-08-05)

- Type: Documentation correction (post-independent-audit).
- Status: **Addressed in this rev. 4 docs pass.** The rev. 4 corrections are applied to the gap analysis and ADR-004. The corrections are additive to the rev. 3 block; the rev. 3 traceability is preserved. The rev. 4 corrections are summarized in the architecture docs' rev. 4 block and in `docs/workflow/STATUS.md` "Identity / Organizations / Resource Ownership docs correction (rev. 4 audit-preparation)" and `docs/workflow/NEXT_ACTIONS.md` "Identity / Organizations / Resource Ownership — Rev. 4 audit-preparation (2026-08-05, this mission)".
- The rev. 4 corrections cover: (1) posts actor vs owner (gap §7.4; ADR §7.3); (2) opportunities/school scope (gap §7.1/§7.4; ADR §6.7/§7.3); (3) contact requests and contact authorizations (gap §7.4; ADR §7.3); (4) views/grants/security with minimum grants on view and base (gap §13.4; ADR §7.7/§8 I-20); (5) owner transfer atomicity (gap §5.4; ADR §8 I-18); (6) gates vocabulary (gap §17.0; ADR §15.0); (7) `company_profiles` single coherent option (gap §4.4; ADR §7.2); (8) status vocabulary (`active | invited | suspended | disabled`); (9) `school_id` matrix exhaustive and concrete (gap §9.4); (10) internal contradictions fixed (projection table exactly 6 rows; "five vs six" → six; §15.6 / §15.13 summary vs detailed); (11) ADR acceptance posture (gap §17.A; ADR §15.A).
- **Active state (rev. 4).** FASE 0 COMPLETE; ADR READY FOR OWNER ACCEPTANCE (Gate A; Proposed — OWNER ACCEPTANCE REQUIRED; rev. 4 does not auto-Accept); GAP ANALYSIS READY FOR AUDIT (APPROVED pendiente auditoría; rev. 4); **CORE schema-only: READY (documental; implementación bloqueada)** (Gate B's documentary gate is READY; the implementation is BLOCKED by Gate D and by the owner acceptance of ADR-004); PUBLISHING READY WITH DEFERRED ITEMS; INTERACTIONS BLOCKED D-OD-1 (D-OD-1 is the only blocker for Gate C; D-OD-2 / D-OD-3 are sub-decisions absorbed by D-OD-1; D-OD-6 / D-OD-7 are implementation / runtime notes, not independent gates in this classification); MIGRATIONS BLOCKED; IMPLEMENTATION BLOCKED; SUPABASE UNCHANGED.
- **No validations are claimed in this docs pass.** The docs pass is documentation-only; no `npm`, no `test`, no `lint`, no `typecheck`, no `build`, no SQL, no Supabase call, no MCP mutation, no migration was executed. The validation of the rev. 4 corrections is the responsibility of the independent audit (Gap Analysis) and the product owner (ADR-004 Gate A acceptance); it is not claimed in this docs pass. The orchestrator may run the three git commands (`git diff --check`, `git status --short --branch`, `git diff --stat`) to confirm the diff is whitespace-clean, the branch state is `stabilization/release-readiness` and the diff stat is consistent with the additive references; no other validations are claimed.
- The historical blocks in the four workflow docs (STATUS, NEXT_ACTIONS, SESSION_LOG, OPEN_QUESTIONS) are preserved; only additive blocks are added. The rev. 4 update is additive to the rev. 3 block.
- The next session awaits the **owner acceptance of ADR-004 (Gate A)** and the **independent audit of the gap analysis**. Phase 1B (schema-only) and all migrations remain **BLOCKED** by Gate B, Gate C and Gate D. No commit, no push.

## Q10 - Identity / Organizations / Resource Ownership docs correction (rev. 4.1, second-audit residue cleanup, 2026-08-05)

> **HISTORICAL — SUPERSEDED BY REV 4.2 (Q11, this session).** The rev. 4.1 framing in this Q10 entry is preserved for traceability but is **not** the canonical state. Specifically, the rev. 4.1 wording "CORE DOCUMENTARY PACKAGE: READY FOR AUDIT" / "GATE B EXECUTION: BLOCKED" / "GATE D MIGRATIONS: BLOCKED" is **SUPERSEDED BY REV 4.2**: Gate B is split into B1 (Core schema design, READY AFTER OWNER ACCEPTANCE) and B2 (Core migration readiness, BLOCKED). The canonical state is recorded in Q11 and in `docs/workflow/STATUS.md` "Identity / Organizations / Resource Ownership docs correction (rev. 4.2, third-pass corrections, Part A, this mission)".

- Type: Documentation correction (post-second-independent-audit; rev. 4.1).
- Status: **Addressed in this rev. 4.1 docs pass** (historical; the canonical state is rev. 4.2, Q11). The rev. 4.1 corrections are applied to the gap analysis and ADR-004. The rev. 4.1 corrections are additive to the rev. 4 block; the rev. 4 and rev. 3 traceability are preserved. The rev. 4.1 corrections are summarized in the architecture docs' rev. 4.1 block.
- The rev. 4.1 corrections address the **five residues** flagged by the second independent audit. The summary is preserved as historical. The canonical state is the rev. 4.2 corrections in Q11.
- **Active state (rev. 4.1, historical).** FASE 0 COMPLETE; ADR-004 Proposed / READY FOR OWNER ACCEPTANCE; GAP ANALYSIS READY FOR AUDIT; **CORE DOCUMENTARY PACKAGE: READY FOR AUDIT**; **GATE B EXECUTION: BLOCKED**; PUBLISHING READY WITH DEFERRED ITEMS; INTERACTIONS BLOCKED exclusively D-OD-1; GATE D MIGRATIONS: BLOCKED; MIGRATIONS BLOCKED; IMPLEMENTATION BLOCKED; SUPABASE UNCHANGED; D-01..D-43 preserved; ADR Proposed — OWNER ACCEPTANCE REQUIRED. The three-state framing is **SUPERSEDED BY REV 4.2** (Q11).
- **Rev. 3 framings NOT in the canonical state.** The following rev. 3 framings inside Q6 and Q8 are explicitly **not** part of the canonical state: D-OD-2 / D-OD-3 / D-OD-6 as independent gates for Gate C; the 10-row projection table; `company_profiles` single-table view as the target. The canonical state for the doc corrections is rev. 4.2 (Q11).
- The next session awaits the **owner acceptance of ADR-004 (Gate A)** and the **independent audit of the gap analysis** (rev. 4.2). Phase 1B (schema-only) and all migrations remain **BLOCKED** by Gate B1 / B2 / C / D. No commit, no push.

## Q11 - Identity / Organizations / Resource Ownership docs correction (rev. 4.2, third-pass corrections, Part A, HISTORICAL PRE-ACCEPTANCE, 2026-08-05)

> **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING (Q13, this session).** The rev. 4.2 framing in this Q11 entry is preserved for traceability as historical but is **not** the canonical state. The post-rev. 4.2 audit detected eleven active residues that are addressed in Q12 (cleanup) and in Q13 (FINAL DOCUMENTARY CLEANUP / AUDIT PENDING with in-place corrections). The canonical state is recorded in the **"Canonical active state" section at the top of this file** and in Q13, and in `docs/workflow/STATUS.md` "Canonical active state (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING)" at the top. The historical "four levels" / "four-gate model" / "in parallel" / "in flight" / "Core schema-only: READY" wording is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**; the canonical framing is five states (A + B1 + C + B2 + D) and the strict sequence A → B1 → C → B2 → D. **A.1–A.7 are NOT declared corrected/closed in this pass.**

- Type: Documentation correction (post-third-pass; rev. 4.2; Part A; historical).
- Status: **Addressed in this rev. 4.2 docs pass (historical; the canonical state is rev. 4.2 cleanup, Q12).** The rev. 4.2 corrections are applied to the gap analysis and ADR-004. The rev. 4.2 corrections are additive to the rev. 4.1 block; the rev. 4.1 / rev. 4 / rev. 3 traceability are preserved. The rev. 4.2 corrections are summarized in the architecture docs' rev. 4.2 block and in `docs/workflow/STATUS.md` and `docs/workflow/NEXT_ACTIONS.md`.
- The rev. 4.2 corrections address the **seven corrections** of the third-pass brief (A.1..A.7):
  1. **A.1 — `student_enrollments` is CANONICAL (live + history).** D-15 is rewritten. Conceptual model `id, student_profile_id, school_organization_id, status, is_primary, started_at, ended_at, created_by_profile_id, created_at, updated_at` with `status` initial values `pending, active, completed, transferred, withdrawn`. At-most-one primary active invariant recorded. New policies and new server actions do not consult `student_profiles.school_id` for authorization. Matrix future row uses `student_enrollments.student_profile_id + school_organization_id` (not `student_enrollments.school_id`). Current absence and target conceptual shape are differentiated. `student_profiles.school_id` is a compatibility / adapter column. `profiles.school_id` is legacy / non-authoritative. Gap analysis §9.1, §9.2, §9.4, §15.8; ADR §6.4, §7.1, §11 Step 7, §8 I-17.
  2. **A.2 — `created_by_profile_id` is AUDIT ONLY.** Authorization source list and audit source list are disjoint. D-08, D-39, D-40, D-41, §6.10, §10, §13.4, I-12, I-15, I-19 are reformulated. Company A example recorded. D-41 (rev. 4.2) states: "La autorización organizacional depende de `memberships` activas, ownership del recurso y relaciones explícitas de dominio. `created_by_profile_id` registra el actor histórico y no autoriza." Historical "triad" framing marked **HISTORICAL — SUPERSEDED BY REV 4.2**. Gap analysis §5.5, §10; ADR §6.10, §7.6, §15.A.5.
  3. **A.3 — `organizations` has no canonical `profile_id`.** Canonical shape `id, kind, legal_name, display_name, slug, description, logo_url, status, created_at, updated_at, …` with `kind IN ('school', 'company')`. Human relationship lives only in `organization_memberships`. Compatibility: `company_profiles` / `schools` are physical adapters; `organization_legacy_links` is a documented alternative (no table created). No future policy reads `organizations.profile_id`. Gap analysis §4.1..§4.4, §7.2, §15.2 (Step 1); ADR §7.2, §11 Step 1, §8 I-5.
  4. **A.4 — Exactly one active owner.** §5.4 records the **8-step conceptual transfer sequence** (no SQL): (1) lock the organization and its memberships, (2) verify the current owner, (3) verify the successor, (4) create / activate the successor's membership **without** `owner`, (5) neutralize the previous owner in the same transaction, (6) promote the successor in the same transaction, (7) register the audit event, (8) verify exactly one active owner before commit. Atomicity preserved: no two active owners, no active organization without an owner, no auto-promotion on suspension, revocation requires prior transfer or organizational suspension. The exactly-one invariant is the combination of at-most-one and at-least-one. Pending / suspended / recovery states have no ordinary operations. Gap analysis §5.4; ADR §7.2, §8 I-18.
  5. **A.5 — Real semantics of views / functions.** "security_definer view" is **not** a category. Three real alternatives: (i) `security_invoker = true` view + minimum grants on view and base + column allowlist + RLS; (ii) owner-behavior view (dedicated role, no `BYPASSRLS`, not mutable by app users; **not** called "security_definer view"); (iii) `SECURITY DEFINER` function with fixed `search_path`, allowlisted structured output, minimum `EXECUTE`, audit. Documentary recommendation by projection type: public = prefer `SECURITY DEFINER` function (or owner-behavior view documented as such); authenticated = `security_invoker = true` view; internal compatibility = `security_invoker = true` view by default. Runtime grants are Gate D. Gap analysis §13.4; ADR §7.7.
  6. **A.6 — Gate B1 / B2 split.** §17 is reformulated. **Gate B1 (Core schema design)**: ADR Accepted; contracts updated; entities and relationships documented; conceptual constraints listed; ownership matrix and authorization matrix produced; audit-only reiteration in the documents. **No staging, no secrets, no runtime, no migrations, no backfill, no SQL, no Supabase**. B1 opens as soon as the ADR is **Accepted**. **Gate B2 (Core migration readiness)**: B1 closed; staging project provisioned; CI Node 22 result recorded; surgical fixes merged; Core migration plan authored / reviewed / audited with idempotent backfill, RLS impact, rollback, staging verification; local tests green; critical fixes closed; plan internally audited. B2 remains blocked until B1 is closed and the preconditions are met. Gate C remains blocked exclusively by D-OD-1. Gate D remains blocked. Gap analysis §17; ADR §15.B.
  7. **A.7 — Workflow and audit.** The four workflow docs are updated to rev. 4.2. `DECISION_LOG` is not updated in this pass. No validations are claimed; no commit / push is made. The orchestrator runs only `git diff --check`, `git status --short --branch`, `git diff --stat` at the end of Part A.
- **Active state (rev. 4.2) — canonical verdict posture.**
  - **FASE 0 COMPLETE**.
  - **GAP ANALYSIS READY FOR AUDIT** (rev. 4.2; APPROVED pendiente auditoría).
  - **ADR-004 Proposed / READY FOR OWNER ACCEPTANCE** (Gate A; **Proposed — OWNER ACCEPTANCE REQUIRED**; rev. 4.2 does **not** auto-Accept).
  - **OWNER ACCEPTANCE READY** (documents in place; the owner acceptance is the Gate A decision).
  - **CORE SCHEMA DESIGN (Gate B1) — READY AFTER OWNER ACCEPTANCE** (rev. 4.2). B1 opens as soon as the ADR is **Accepted**.
  - **CORE MIGRATION READINESS (Gate B2) — BLOCKED** (rev. 4.2). B2 requires the owner acceptance, the staging project, the CI Node 22 result, the surgical fixes, the Core migration plan, the local green tests and the audit.
  - **PUBLISHING READY WITH DEFERRED ITEMS** (documentary).
  - **INTERACTIONS BLOCKED exclusively D-OD-1** (Gate C is blocked exclusively by D-OD-1 as a decision; C.1 / C.2 / C.5 are preparation controls, not additional decisions).
  - **GATE D MIGRATIONS: BLOCKED** (until A / B1 / B2 / C closed + D.5 baseline remote + D.6 design audit + D.7 backfill / rollback / staging verification + D.8 RLS runtime + D.9 production authorized + D.10 audit dictums closed; Gate D is never "in parallel" with A / B / C).
  - **MIGRATIONS BLOCKED** / **IMPLEMENTATION BLOCKED** / **SUPABASE UNCHANGED**.
  - **D-01..D-43 preserved**; **ADR Proposed — OWNER ACCEPTANCE REQUIRED**; no claim of Accepted or migrations ready.
- **Rev. 4.1 framings NOT in the canonical state.** The following rev. 4.1 framings inside Q10 are explicitly **not** part of the canonical state (rev. 4.2):
  - **"CORE DOCUMENTARY PACKAGE: READY FOR AUDIT" / "GATE B EXECUTION: BLOCKED" / "GATE D MIGRATIONS: BLOCKED" three-state framing** — replaced by the B1 / B2 split above.
  - **The `security_definer` controlled exception as a per-row mode for the 6 public projections** — replaced by the three real alternatives in A.5; the controlled public projection pattern is preserved (prefer `SECURITY DEFINER` function), but the wording is sharpened.
  - **The former transfer-flow wording** — replaced by the 8-step sequence in A.4. This entry is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**.
  - **The triad framing of D-41** — replaced by the disjoint authorization / audit source lists in A.2.
  - **`organizations.profile_id` as a canonical adapter / backfill column** — replaced by "no canonical `profile_id`" in A.3.
- **No validations are claimed in this docs pass.** The docs pass is documentation-only; no `npm`, no `test`, no `lint`, no `typecheck`, no `build`, no SQL, no Supabase call, no MCP mutation, no migration, no staging, no runtime grant, no fixture was executed. The validation of the rev. 4.2 corrections is the responsibility of the independent audit (Gap Analysis) and the product owner (ADR-004 Gate A acceptance); it is **not** claimed in this docs pass. The orchestrator may run the three git commands (`git diff --check`, `git status --short --branch`, `git diff --stat`) to confirm the diff is whitespace-clean, the branch state is `stabilization/release-readiness` and the diff stat is consistent with the additive references; no other validations are claimed.
- The next session awaits the **owner acceptance of ADR-004 (Gate A)** and the **independent audit of the gap analysis** (rev. 4.2). Phase 1B (schema-only) and all migrations remain **BLOCKED** by Gate B1 / B2 / C / D. No commit, no push.

## Q12 - Identity / Organizations / Resource Ownership docs correction (HISTORICAL PRE-ACCEPTANCE: rev. 4.2 cleanup / audit pending, 2026-08-05)

> **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING (Q13, this session).** The rev. 4.2 cleanup block is preserved for traceability as the **first** cleanup pass. The **canonical** state is the **"Canonical active state" section at the top of this file** and the **Q13 (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING)** entry below, which records the **in-place corrections** to the canonical sections of the architecture docs (the previous rev. 4.2 cleanup only added an override block and did not edit the canonical sections in-place). The rev. 4.1 / rev. 4 / rev. 3 / rev. 4.2 / rev. 4.2 cleanup traceability is preserved. **The canonical state is "FINAL DOCUMENTARY CLEANUP / AUDIT PENDING", not "audited" or "closed".** **A.1–A.7 are NOT declared corrected/closed in this pass.** No validations are claimed; no commit / push is made. SUPABASE UNCHANGED. The historical "four levels" / "four-gate model" / "in parallel" / "in flight" / "Core schema-only: READY" / "A.1–A.7 corrected/closed" wording is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**; the canonical framing is the five states (A + B1 + C + B2 + D) and the strict sequence A → B1 → C → B2 → D, with the "A.1–A.7 NOT closed" stance.

- Type: Documentation correction (post-audit; rev. 4.2 cleanup).
- Status: **Addressed in this rev. 4.2 cleanup docs pass (CLEANUP / AUDIT PENDING; not yet audited).** The rev. 4.2 cleanup is applied to the gap analysis and ADR-004. The rev. 4.2 cleanup is additive to the rev. 4.2 block; the rev. 4.2 / rev. 4.1 / rev. 4 / rev. 3 traceability are preserved. The rev. 4.2 cleanup is summarized in the architecture docs' rev. 4.2 cleanup block (§16 in the ADR, §20 in the gap analysis) and in `docs/workflow/STATUS.md` "Identity / Organizations / Resource Ownership docs correction (rev. 4.2 cleanup / audit pending, this session)" and in `docs/workflow/NEXT_ACTIONS.md` "Identity / Organizations / Resource Ownership — Rev. 4.2 cleanup / audit pending (2026-08-05, this session)".
- **The eleven residues** (the post-rev. 4.2 audit). (1) D-15 — `student_enrollments` is canonical (live + history); `student_profiles.school_id` is a single source-of-read adapter, kept in sync by a single adapter layer; `profiles.school_id` is legacy / non-authoritative. (2) `created_by_profile_id` is audit only; the authorization / audit lists are disjoint; D-41 (canonical) states: "La autorización organizacional depende de `memberships` activas, ownership del recurso y relaciones explícitas de dominio. `created_by_profile_id` registra el actor histórico y no autoriza."; Company A example preserved. (3) `organizations` has no canonical `profile_id`; `schools.profile_id` / `company_profiles.profile_id` are physical legacy adapter columns; `organization_legacy_links` is a documented alternative (no table created); no future policy reads `organizations.profile_id`. (4) Ownership — 8-step conceptual sequence (not 7); at-most-one, at-least-one, exactly-one; `organizations.status = 'active'` ⇒ exactly one active owner; pending / suspended / recovery states have no ordinary operations; no two owners, no active org ownerless, no auto-promotion; revocation only by prior transfer or organization suspension. (5) Views / functions — no "security_definer view" wording in active text; three real alternatives: (i) `security_invoker = true` view + minimum grants on view and base + column allowlist + RLS; (ii) owner-behavior view (dedicated role, no `BYPASSRLS`, allowlist, minimum grants, not mutable by app users; **not** called "security_definer view"); (iii) `SECURITY DEFINER` function with fixed `search_path`, allowlisted structured output, minimum `EXECUTE`, audit. Recommendation: public projections = `SECURITY DEFINER` function (no view grants to `anon`; `EXECUTE` minimum); authenticated projections = `security_invoker = true` view; internal compatibility views = `security_invoker = true` view by default. The 6 public rows of the public projection table record the function / `EXECUTE` for public, not "security_definer view" wording, and no view grants are recorded for the public surface. Runtime grants are Gate D. (6) Gates — sequence A → B1 → C → B2 / D, no "in parallel" wording; D-OD-1 is the only decisional blocker for C; B2 is a preparation gate (readiness), not a decisional blocker. (7) Workflow — "CLEANUP / AUDIT PENDING" state, not "A.1–A.7 corrected/closed" as audit final. (8) `student_school_records` — optional / deferred Core extension, not authority for enrollment / authorization; if in B1 / B2 lists, `student_school_records.student_enrollment_id → student_enrollments.id`, no independent `school_id`; field contract pending B1, not prerequisite of Gate A. (9) Adapters / views rollback — physical legacy tables not dropped during expand; mappings / backups verifiable; adapters retired only after consumers / FKs / policies migrated; rollback restores adapter physical / mappings / previous reads, no SQL claimed. (10) D-OD-4 — operational choice, not Gate C blocker, not independent blocker; D-OD-5 may continue deferred for company roles, only B2 / implementation if applies. (11) D-01..D-43 intact; ADR Proposed; no SQL / code / Supabase.
- **Active state (rev. 4.2 cleanup / audit pending) — canonical verdict posture.**
  - **FASE 0 COMPLETE**.
  - **GAP ANALYSIS READY FOR AUDIT** (rev. 4.2 cleanup; APPROVED pendiente auditoría).
  - **ADR-004 rev. 4.2 Proposed / READY FOR OWNER ACCEPTANCE** (Gate A; **Proposed — OWNER ACCEPTANCE REQUIRED**; rev. 4.2 cleanup does **not** auto-Accept).
  - **OWNER ACCEPTANCE READY** (the documents are in place; the owner acceptance is the Gate A decision; the cleanup is "READY" in the sense of ready-for-owner-decision, not "approved").
  - **CORE SCHEMA DESIGN (Gate B1) — READY AFTER OWNER ACCEPTANCE** (rev. 4.2 cleanup). B1 opens as soon as the ADR is **Accepted**.
  - **CORE MIGRATION READINESS (Gate B2) — BLOCKED** (rev. 4.2 cleanup). B2 is a prerequisite of any migration.
  - **PUBLISHING READY WITH DEFERRED ITEMS** (documentary).
  - **INTERACTIONS BLOCKED exclusively D-OD-1** (Gate C is blocked exclusively by D-OD-1 as a decision; C.1 / C.2 / C.5 are preparation controls, not additional decisions).
  - **GATE D MIGRATIONS: BLOCKED** (until A / B1 / B2 / C closed; Gate D is never "in parallel" with A / B / C).
  - **MIGRATIONS BLOCKED** / **IMPLEMENTATION BLOCKED** / **SUPABASE UNCHANGED**.
  - **D-01..D-43 preserved**; **ADR Proposed — OWNER ACCEPTANCE REQUIRED**; no claim of Accepted or migrations ready.
- **Rev. 4.2 framings NOT in the canonical state (rev. 4.2 cleanup).** The following rev. 4.2 framings inside Q11 are explicitly **not** part of the canonical state (rev. 4.2 cleanup):
  - **"A.1–A.7 corrected/closed" framing** — replaced by "CLEANUP / AUDIT PENDING" (the cleanup is not yet audited).
  - **"in parallel" wording for B2 / C in §17.C** — replaced by the strict sequence A → B1 → C → B2 / D.
  - **The former transfer-flow residue** — replaced by the 8-step sequence. This entry is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**.
  - **"security_definer view" wording in the public projection table** — replaced by the function / `EXECUTE` for public rows.
  - **`organizations.profile_id` as a canonical adapter / backfill column** — replaced by "no canonical `profile_id`".
  - **D-OD-4 as Gate C / independent blocker** — replaced by D-OD-4 as operational choice, not Gate C blocker, not independent blocker.
- **No validations are claimed in this docs pass.** The docs pass is documentation-only; no `npm`, no `test`, no `lint`, no `typecheck`, no `build`, no SQL, no Supabase call, no MCP mutation, no migration, no staging, no runtime grant, no fixture was executed. The audit of the rev. 4.2 cleanup is the responsibility of the independent audit (Gap Analysis) and the product owner (ADR-004 Gate A acceptance); it is **not** claimed in this docs pass. The orchestrator may run the three git commands (`git diff --check`, `git status --short --branch`, `git diff --stat`) to confirm the diff is whitespace-clean, the branch state is `stabilization/release-readiness` and the diff stat is consistent with the additive references; **no other validations are claimed**.
- The next session awaits the **owner acceptance of ADR-004 (Gate A)** and the **independent audit of the gap analysis (rev. 4.2 cleanup)**. Phase 1B (schema-only) and all migrations remain **BLOCKED** by Gate B1 / B2 / C / D. No commit, no push.

## Q13 - Identity / Organizations / Resource Ownership docs correction (REV 4.2 pre-acceptance historical)

> **HISTORICAL PRE-ACCEPTANCE STATE.** Q12 and Q13 preserve the prior documentary-cleanup traceability. The active accepted state is Q14 and the canonical state at the top of this file. No active gate or acceptance depends on the historical wording below.

- Type: Documentation correction (post-audit; REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING; Part A; final correction).
- Status: **Addressed in this REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING docs pass (FINAL DOCUMENTARY CLEANUP / AUDIT PENDING; not yet audited).** The **in-place corrections** are applied to the canonical sections of the gap analysis and ADR-004. The **REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING** is additive to the rev. 4.2 cleanup; the rev. 4.2 cleanup / rev. 4.2 / rev. 4.1 / rev. 4 / rev. 3 traceability are preserved. The **REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING** is summarized in the architecture docs' §17 (ADR) and §21 (gap analysis) blocks and in the four workflow docs (STATUS.md, NEXT_ACTIONS.md, OPEN_QUESTIONS.md, SESSION_LOG.md).

- **The eleven residues (in-place corrections).** (1) D-15 — `student_enrollments` is canonical (live + history); `student_profiles.school_id` is a single source-of-read adapter, kept in sync by a single adapter layer; `profiles.school_id` is legacy / non-authoritative. (2) `created_by_profile_id` is audit only; the authorization / audit lists are disjoint; D-41 (canonical) is preserved verbatim; Company A example preserved. (3) `organizations` has no canonical `profile_id`; `schools.profile_id` / `company_profiles.profile_id` are physical legacy adapter columns; `organization_legacy_links` is a documented alternative (no table created); no future policy reads `organizations.profile_id`. (4) Ownership — **8-step conceptual sequence** (not 7); at-most-one, at-least-one, exactly-one; `organizations.status = 'active'` ⇒ exactly one active owner; **pending / suspended / recovery states have no ordinary operations**; no two owners, no active org ownerless, no auto-promotion; revocation only by prior transfer or organization suspension. (5) Views / functions — no "security_definer view" wording in active text; three real alternatives: (i) `security_invoker = true` view + minimum grants on view and base + column allowlist + RLS; (ii) owner-behavior view (dedicated role, no `BYPASSRLS`, allowlist, minimum grants, not mutable by app users; **not** called "security_definer view"); (iii) `SECURITY DEFINER` function with fixed `search_path`, allowlisted structured output, minimum `EXECUTE`, audit. **Public projections = `SECURITY DEFINER` functions** (6 public rows, function output and `EXECUTE`, **not "view grants"**); authenticated projections = `security_invoker = true` view; internal compatibility views = `security_invoker = true` view by default. `security_barrier` only in views where applicable, not in functions. Runtime grants are Gate D. (6) Gates — **canonical sequence A → B1 → C → B2 → D**; D-OD-1 is the only decisional blocker for C; B2 is a preparation gate (readiness), not a decisional blocker of C; the "in parallel" / "in flight" wording is HISTORICAL. (7) Workflow — "**FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**" state, **NOT "A.1–A.7 corrected/closed"** as audit final. (8) `student_school_records` — optional / deferred Core extension; if in B1 / B2, `student_school_records.student_enrollment_id → student_enrollments.id`, no independent `school_id`; field contract pending B1, **NOT a prerequisite of Gate A**. (9) Adapters / views rollback — physical legacy tables not dropped during expand; mappings / backups verifiable; adapters retired only after consumers / FKs / policies migrated; rollback restores adapter physical / mappings / previous reads / **history of enrollments preserved**, **no SQL claimed**. (10) D-OD-4 — operational choice, not Gate C / Gate A criterion, not independent blocker; D-OD-5 deferred for company roles, only Gate B concern, not Gate A criterion. (11) D-01..D-43 intact; ADR Proposed; no SQL / code / Supabase.

- **Active state (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING, canonical).**
  - **FASE 0 COMPLETE**.
  - **GAP ANALYSIS READY FOR AUDIT** (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING; APPROVED pendiente auditoría).
  - **ADR-004 rev. 4.2 Proposed / READY FOR OWNER ACCEPTANCE** (historical pre-acceptance state).
  - **OWNER ACCEPTANCE READY** (historical pre-acceptance state).
  - **CORE SCHEMA DESIGN (Gate B1) — READY AFTER OWNER ACCEPTANCE** (historical pre-acceptance state).
  - **CORE MIGRATION READINESS (Gate B2) — BLOCKED** (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING). B2 is a prerequisite of any migration.
  - **PUBLISHING READY WITH DEFERRED ITEMS** (documentary).
  - **INTERACTIONS BLOCKED exclusively D-OD-1** (Gate C is blocked exclusively by D-OD-1 as a decision; C.1 / C.2 / C.5 are preparation controls, not additional decisions).
  - **GATE D MIGRATIONS: BLOCKED** (until A / B1 / B2 / C closed; Gate D is never "in parallel" with A, B1, B2 or C).
  - **MIGRATIONS BLOCKED** / **IMPLEMENTATION BLOCKED** / **SUPABASE UNCHANGED**.
  - **D-01..D-43 fixed and preserved**; **D-OD-1..D-OD-7 deferred**; this historical block made no claim of migrations ready.

## Q14 - Identity / Organizations / Resource Ownership — owner acceptance

- Type: Architectural decision.
- Status: **Answered / Resolved**.
- Answer: The owner accepted ADR-004 rev. 4.2 on 2026-08-05 on branch `stabilization/release-readiness` with the exact quote: “Apruebo ADR-004 rev. 4.2.”
- Pointer: Formal acceptance in `docs/architecture/ADR/ADR-004-identity-organizations-and-resource-ownership.md`, acceptance entry in `docs/workflow/SESSION_LOG.md`, and formal decision in `docs/workflow/DECISION_LOG.md`.
- Remaining gates: B1 is READY / OPENED DOCUMENTALLY and is not implemented; B2 is BLOCKED; C is BLOCKED exclusively by D-OD-1; D, migrations and implementation are BLOCKED. D-OD-1..D-OD-7 remain deferred and open.

- **Rev. 4.2 cleanup framings NOT in the canonical state (REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING).** The following rev. 4.2 cleanup framings inside Q12 are explicitly **not** part of the canonical state:
  - **"CLEANUP / AUDIT PENDING"** — replaced by "**FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**" (the previous cleanup was additive to the file as a separate block; the in-place corrections are now applied to the canonical sections).
  - **"in parallel" / "in flight" wording for B2 / C** — replaced by the strict sequence **A → B1 → C → B2 → D**.
  - **The former transfer-flow residue** — replaced by the 8-step sequence. This entry is **HISTORICAL — SUPERSEDED BY REV 4.2 FINAL DOCUMENTARY CLEANUP / AUDIT PENDING**.
  - **"security_definer view" wording in the public projection table** — replaced by `SECURITY DEFINER` functions (6 public rows, function output and `EXECUTE`, not "view grants").
  - **`organizations.profile_id` as a canonical adapter / backfill column** — replaced by "**no canonical `profile_id`**".
  - **D-OD-4 as Gate C / independent blocker** — replaced by D-OD-4 as operational choice, not Gate C blocker, not Gate A criterion, not independent blocker. D-OD-5 is deferred for company roles, only Gate B concern, not Gate A criterion.

- This documentary pass runs no `npm`, tests, lint, typecheck, build, SQL, Supabase, migration, staging, runtime or implementation validation. The acceptance and approved documentary state are recorded in ADR-004, `SESSION_LOG.md` and `DECISION_LOG.md`.

- The next action is to update the architectural contracts and design the documentary B1 package. **B1 is READY / OPENED DOCUMENTALLY; B2 is BLOCKED; Gate C is blocked exclusively by D-OD-1; executable migrations and implementation remain blocked by Gate D.** The documentary commit is still being prepared from the seven-file acceptance/versioning set. Part B (Implementation) is **NOT** in scope for this docs pass.
